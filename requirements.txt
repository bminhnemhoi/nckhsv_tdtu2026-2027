# Core pipeline -- CPU only, no GPU required
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
mne>=1.5              # EDF reader
wfdb>=4.1             # PhysioNet annotations and records

# Figure and document generation (de_cuong_latex/)
matplotlib>=3.7
pymupdf>=1.23         # PDF inspection and rasterisation
python-docx>=1.1      # DOCX verification
pillow>=10.0

# Tests
pytest>=7.4

# External tools, NOT installable via pip -- needed only to rebuild documents:
#   xelatex   (MiKTeX or TeX Live)  -- compiles the LaTeX proposal
#   pandoc    (>= 3.0)              -- exports PDF sources to DOCX
